# Trim Area CCCI service
service ta_ccci_service /system/vendor/bin/ta_ccci_service
    user root
    # The Oreo domain is absent from the Android 10 policy.  The device runs
    # permissive, so let init launch this legacy service in its own context.
    disabled
